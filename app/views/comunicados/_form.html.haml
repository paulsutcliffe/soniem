= form_for @comunicado, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @comunicado.errors.any?
    #error_explanation
      %h2= "#{pluralize(@comunicado.errors.count, "error")}"
      %ul
        - @comunicado.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :titulo, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :titulo, class: 'form-control'
  .form-group
    = f.label :contenido, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_area :contenido, class: 'form-control'
  .form-group
    = f.label :fecha, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.date_select :fecha, class: 'form-control'
  .form-group
    = f.label :pdf, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :pdf, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
