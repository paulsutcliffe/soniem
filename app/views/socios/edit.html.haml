- page_title!('Editando socio')

#region-socios.container

  .region
    .region-title
      %h1
        Editando
        %b
          socio

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @socio, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
