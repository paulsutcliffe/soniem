= form_for @imagen, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @imagen.errors.any?
    #error_explanation
      %h2= "#{pluralize(@imagen.errors.count, "error")}"
      %ul
        - @imagen.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :imagen, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :imagen, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
