= form_for @convenio, :html => { class: 'form-horizontal', role: 'form' } do |f|
  - if @convenio.errors.any?
    #error_explanation
      %h2= "#{pluralize(@convenio.errors.count, "error")}"
      %ul
        - @convenio.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :nombre, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :nombre, class: 'form-control'
  .form-group
    = f.label :descripcion, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_area :descripcion, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
