- page_title!('Editando convenio')

#region-convenios.container

  .region
    .region-title
      %h1
        Editando
        %b
          convenio

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @convenio, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
