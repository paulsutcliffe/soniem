- page_title!('Editando pregunta')

#region-preguntas.container

  .region
    .region-title
      %h1
        Editando
        %b
          pregunta

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @pregunta, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
