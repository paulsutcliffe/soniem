- page_title!('Editando directivo')

#region-directivos.container

  .region
    .region-title
      %h1
        Editando
        %b
          directivo

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @directivo, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
