- page_title!('Editando formato')

#region-formatos.container

  .region
    .region-title
      %h1
        Editando
        %b
          formato

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @formato, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
