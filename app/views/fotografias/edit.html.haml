- page_title!('Editando fotografia')

#region-fotografias.container

  .region
    .region-title
      %h1
        Editando
        %b
          fotografia

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @fotografia, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
